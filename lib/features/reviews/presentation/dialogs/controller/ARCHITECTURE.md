# 📐 Arquitetura Visual - ReviewDialog Controller

## 🏗️ Visão Geral da Arquitetura

```
┌─────────────────────────────────────────────────────────────────┐
│                         UI LAYER (Flutter)                       │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              ReviewDialog Widget                          │  │
│  │  - ChangeNotifierProvider                                │  │
│  │  - Consumer<ReviewDialogController>                      │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                   │
│                              ▼                                   │
└─────────────────────────────────────────────────────────────────┘
                               │
                               │ notifyListeners()
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                    CONTROLLER LAYER                              │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │        ReviewDialogController (Orquestrador)            │  │
│  │                                                          │  │
│  │  📍 Responsabilidades:                                   │  │
│  │    • Coordenar serviços                                  │  │
│  │    • Gerenciar ChangeNotifier                            │  │
│  │    • Batch updates (evitar rebuilds)                     │  │
│  │    • Expor API para UI                                   │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                   │
│                              │ delegates to                      │
│                              ▼                                   │
│         ┌────────────────────┴────────────────────┐             │
│         │                                          │             │
│         ▼                                          ▼             │
│  ┌─────────────┐                          ┌─────────────┐       │
│  │   STATE     │                          │  SERVICES   │       │
│  └─────────────┘                          └─────────────┘       │
└─────────────────────────────────────────────────────────────────┘
         │                                          │
         ▼                                          ▼
┌─────────────────────────────────────────────────────────────────┐
│                      SERVICE LAYER                               │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │           ReviewDialogState (📊 Estado Puro)             │  │
│  │                                                          │  │
│  │  • eventId, reviewerId, reviewerRole                     │  │
│  │  • currentStep, presenceConfirmed                        │  │
│  │  • ratingsPerParticipant (owner)                         │  │
│  │  • ratings, selectedBadges (participant)                 │  │
│  │  • Getters computados (isOwnerReview, progress, etc)    │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │      ReviewValidationService (✅ Validações)             │  │
│  │                                                          │  │
│  │  • canProceed()                                          │  │
│  │  • hasCompletedRatings()                                 │  │
│  │  • hasEvaluatedAllParticipants()                         │  │
│  │  • validateAllParticipantsReviewed()                     │  │
│  │  • canGoBack()                                           │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │         ReviewUIService (🎨 Apresentação)                │  │
│  │                                                          │  │
│  │  • getStepLabel()                                        │  │
│  │  • getButtonText()                                       │  │
│  │  • shouldShowSkipButton()                                │  │
│  │  • getCurrentRatings()                                   │  │
│  │  • getCurrentBadges()                                    │  │
│  │  • getErrorMessage()                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │      ReviewNavigationService (🧭 Navegação)              │  │
│  │                                                          │  │
│  │  • goToBadgesStep()                                      │  │
│  │  • prepareNextParticipant()                              │  │
│  │  • preparePreviousStep()                                 │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │      ReviewBatchService (📝 Persistência)                │  │
│  │                                                          │  │
│  │  • createReviewBatch()                                   │  │
│  │  • createPendingReviewBatch()                            │  │
│  │  • markParticipantReviewedBatch()                        │  │
│  │  • prepareOwnerData()                                    │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌─────────────────────────────────────────────────────────────────┐
│                       DATA LAYER                                 │
│                                                                  │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              ReviewRepository                             │  │
│  │                                                          │  │
│  │  • createReview()                                        │  │
│  │  • updatePendingReview()                                 │  │
│  │  • saveConfirmedParticipant()                            │  │
│  │  • dismissPendingReview()                                │  │
│  └──────────────────────────────────────────────────────────┘  │
│                              │                                   │
│                              ▼                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │              Cloud Firestore                              │  │
│  │                                                          │  │
│  │  Collections:                                            │  │
│  │    • Reviews                                             │  │
│  │    • PendingReviews                                      │  │
│  │    • Events/ConfirmedParticipants                        │  │
│  │    • Users                                               │  │
│  └──────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Fluxo de Dados (Data Flow)

### 1️⃣ User Interaction → Controller

```
┌──────────┐
│   User   │
│  clicks  │
│  button  │
└────┬─────┘
     │
     ▼
┌────────────────────────┐
│  UI Widget             │
│  ElevatedButton(       │
│    onPressed:          │
│      controller        │
│        .setRating()    │
│  )                     │
└────┬───────────────────┘
     │
     ▼
```

### 2️⃣ Controller → Services

```
┌─────────────────────────┐
│  ReviewDialogController │
│                         │
│  setRating(criterion,   │
│            value) {     │
│    if (isTransitioning) │
│      return;            │
│                         │
│    // Delegate to       │
│    // validation        │
│    if (!ValidationSvc   │
│      .canProceed())     │
│      return;            │
│                         │
│    // Update state      │
│    state.ratings[...]   │
│      = value;           │
│                         │
│    // Notify UI         │
│    notifyListeners();   │
│  }                      │
└─────────────────────────┘
```

### 3️⃣ Services → State

```
┌──────────────────────────┐
│  ValidationService       │
│                          │
│  canProceed(state) {     │
│    switch(              │
│      state.currentStep) │
│    {                    │
│      case ratings:      │
│        return           │
│          hasCompleted   │
│          Ratings(state);│
│    }                    │
│  }                      │
└────┬─────────────────────┘
     │ reads
     ▼
┌──────────────────────────┐
│  ReviewDialogState       │
│                          │
│  currentStep: int        │
│  ratings: Map<String,int>│
│                          │
│  get currentReviewStep   │
│  get progress            │
└──────────────────────────┘
```

### 4️⃣ Controller → Repository → Firestore

```
┌─────────────────────────┐
│  ReviewDialogController │
│                         │
│  submitReview() async { │
│    // Validate          │
│    final errors =       │
│      ValidationSvc      │
│        .validate(state);│
│                         │
│    // Prepare batch     │
│    final batch =        │
│      BatchSvc           │
│        .prepare(state); │
│                         │
│    // Persist           │
│    await _repository    │
│      .createReview(...);│
│  }                      │
└────┬────────────────────┘
     │
     ▼
┌─────────────────────────┐
│  ReviewRepository       │
│                         │
│  createReview() {       │
│    firestore            │
│      .collection(...)   │
│      .doc(...)          │
│      .set(...);         │
│  }                      │
└────┬────────────────────┘
     │
     ▼
┌─────────────────────────┐
│  Cloud Firestore        │
│                         │
│  /Reviews/{id}          │
│    - event_id           │
│    - criteria_ratings   │
│    - badges             │
│    - created_at         │
└─────────────────────────┘
```

---

## 🎯 Separation of Concerns

```
┌────────────────────────────────────────────────────────────────┐
│                    PRESENTATION LAYER                           │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  UI Service                                              │ │
│  │  • Labels, texts, formatação                             │ │
│  │  • Decisões visuais (mostrar/ocultar botões)             │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                     BUSINESS LOGIC LAYER                        │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Validation Service                                      │ │
│  │  • Regras de negócio                                     │ │
│  │  • Validações (mínimo 4 ratings, permissões, etc)        │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Navigation Service                                      │ │
│  │  • Lógica de transição entre steps                       │ │
│  │  • Preparação de dados para navegação                    │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                      DATA ACCESS LAYER                          │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Batch Service                                           │ │
│  │  • Operações em lote (Firestore WriteBatch)              │ │
│  │  • Criação de documentos                                 │ │
│  └──────────────────────────────────────────────────────────┘ │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  Repository                                              │ │
│  │  • CRUD operations                                       │ │
│  │  • Abstração do Firestore                                │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────────┐
│                       STATE LAYER                               │
│  ┌──────────────────────────────────────────────────────────┐ │
│  │  ReviewDialogState                                       │ │
│  │  • Dados puros (sem lógica)                              │ │
│  │  • Getters computados simples                            │ │
│  └──────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────────┘
```

---

## 🔀 Comparação: Antes vs Depois

### ANTES (Monolito)
```
┌─────────────────────────────────────────┐
│  ReviewDialogController (1120 lines)    │
│                                         │
│  ┌─────────────────────────────────┐   │
│  │  • 50+ propriedades             │   │
│  │  • 30+ métodos                  │   │
│  │  • Validação + UI + Persistência│   │
│  │  • Alto acoplamento             │   │
│  │  • Difícil testar               │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
```

### DEPOIS (Modular)
```
┌─────────────────────────────────────────────────────────────┐
│  ReviewDialogController (200 lines) - ORQUESTRADOR          │
│                                                             │
│  ├─► ReviewDialogState (150 lines)                         │
│  │    • Estado puro                                        │
│  │                                                         │
│  ├─► ReviewValidationService (120 lines)                   │
│  │    • Validações isoladas                                │
│  │                                                         │
│  ├─► ReviewUIService (100 lines)                           │
│  │    • Lógica de apresentação                             │
│  │                                                         │
│  ├─► ReviewNavigationService (130 lines)                   │
│  │    • Navegação entre steps                              │
│  │                                                         │
│  └─► ReviewBatchService (110 lines)                        │
│       • Operações Firestore                                │
│                                                             │
│  ✅ Baixo acoplamento                                       │
│  ✅ Alta coesão                                             │
│  ✅ Fácil testar                                            │
└─────────────────────────────────────────────────────────────┘
```

---

## 🧪 Testing Strategy

```
┌──────────────────────────────────────────────────────────────┐
│                      UNIT TESTS                               │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  ReviewValidationService Tests                         │ │
│  │  • test('should validate completed ratings')           │ │
│  │  • test('should validate all participants reviewed')   │ │
│  │  • test('should validate permissions')                 │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  ReviewUIService Tests                                 │ │
│  │  • test('should generate correct step label')          │ │
│  │  • test('should show skip button on comment step')     │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  ReviewNavigationService Tests                         │ │
│  │  • test('should prepare next participant correctly')   │ │
│  │  • test('should handle navigation back')               │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                   INTEGRATION TESTS                           │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  ReviewDialogController Tests                          │ │
│  │  • test('full owner review flow')                      │ │
│  │  • test('full participant review flow')                │ │
│  │  • test('error handling on submit')                    │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────┐
│                     WIDGET TESTS                              │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  ReviewDialog Widget Tests                             │ │
│  │  • test('should display correct step')                 │ │
│  │  • test('should enable/disable buttons correctly')     │ │
│  │  • test('should show error messages')                  │ │
│  └────────────────────────────────────────────────────────┘ │
└──────────────────────────────────────────────────────────────┘
```

---

## 📊 Dependency Graph

```
                     ReviewDialog (Widget)
                            │
                            ▼
              ReviewDialogController (Orquestrador)
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
ReviewDialogState   ReviewValidationService   ReviewUIService
                            │
        ┌───────────────────┼───────────────────┐
        │                   │                   │
        ▼                   ▼                   ▼
ReviewNavigationService   ReviewBatchService   ReviewRepository
                                                    │
                                                    ▼
                                            Cloud Firestore
```

**Legenda:**
- ▼ = depende de (dependency)
- Setas descendentes = fluxo de dados
- Sem setas horizontais = sem acoplamento lateral

---

## 🎓 Design Patterns Utilizados

### 1. **Service Layer Pattern**
- Serviços especializados encapsulam lógica de negócio
- Controller delega para serviços

### 2. **State Pattern**
- `ReviewDialogState` encapsula todo o estado
- Transições controladas por serviços

### 3. **Strategy Pattern**
- Diferentes estratégias para owner vs participant
- `ReviewValidationService.canProceed()` adapta por role

### 4. **Facade Pattern**
- `ReviewDialogController` expõe API simplificada
- Oculta complexidade interna dos serviços

### 5. **Repository Pattern**
- `ReviewRepository` abstrai acesso ao Firestore
- Facilita testes com mocks

---

## 🔐 Separação de Responsabilidades: Backend vs Frontend

### `allowedToReviewOwner` - Caso de Estudo

#### ❌ **Problema Comum: Lógica de Negócio no Frontend**
```dart
// MAU: Frontend decide quem pode avaliar
bool canReview() {
  if (!presenceConfirmed) return false;
  if (alreadyReviewed) return false;
  if (eventExpired) return false;
  return true;
}
```

**Problemas:**
- 🐛 Duplicação de lógica (backend + frontend)
- 🔓 Regras podem divergir
- 🛠️ Mudanças de regra exigem deploy de app
- ⚠️ Bypass possível via API direta

#### ✅ **Implementação Atual: Backend é Fonte de Verdade**

```
┌─────────────┐         ┌──────────────┐         ┌──────────────┐
│   Backend   │ ─────▶  │  Controller  │ ─────▶  │      UI      │
│             │         │              │         │              │
│ Define:     │         │ Apenas lê:   │         │ Apenas exibe:│
│ • Regras    │         │ • allowed    │         │ • Mensagem   │
│ • Permissões│         │ • Valida     │         │ • Bloqueia   │
└─────────────┘         └──────────────┘         └──────────────┘
```

**Implementação:**
```dart
// Backend define (PendingReviewModel)
final bool? allowedToReviewOwner;

// Controller apenas lê e reage
_state.allowedToReviewOwner = pendingReview.allowedToReviewOwner ?? false;

if (!_state.allowedToReviewOwner) {
  _state.errorMessage = 'Você não tem permissão para avaliar este evento';
  return;
}
```

**Vantagens:**
- ✅ Single Source of Truth (Backend)
- ✅ Mudanças de regra não exigem novo deploy
- ✅ Security Rules do Firestore validam novamente
- ✅ Impossível bypass

#### 🚀 **Evolução Futura: Backend Envia Razão**

**Limitação Atual:**
```json
{
  "allowed_to_review_owner": false
  // ❌ Frontend não sabe o motivo
}
```

**Proposta de Melhoria:**
```json
{
  "allowed_to_review_owner": false,
  "denial_reason": "presence_not_confirmed"
}
```

**Frontend converte para mensagem amigável:**
```dart
String _getErrorMessageForReason(String? reason) {
  switch (reason) {
    case 'presence_not_confirmed':
      return 'Sua presença ainda não foi confirmada pelo organizador';
    case 'event_not_attended':
      return 'Você não participou deste evento';
    case 'already_reviewed':
      return 'Você já avaliou este evento';
    case 'event_cancelled':
      return 'Este evento foi cancelado';
    case 'review_period_expired':
      return 'O período para avaliar este evento expirou';
    default:
      return 'Você não tem permissão para avaliar este evento';
  }
}
```

**Benefícios:**
- 📱 Mensagens mais específicas e úteis
- 🌍 Fácil internacionalização (i18n)
- 🎯 Usuário entende exatamente o problema
- 🔧 Manutenção centralizada no backend

---

**Autor:** AI Assistant  
**Data:** 8 de dezembro de 2025  
**Versão:** 2.1
